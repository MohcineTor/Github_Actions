name: test action 
description: 'tes'
inputs:
  platforms:
    description: 'List of platforms to initialize'
    required: true
runs:
  using: 'composite'
  steps:
    - name: test Control
      run: |
          echo "test"
      shell: bash